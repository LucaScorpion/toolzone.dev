export class DistanceConverter {
  public static readonly NAME = 'DistanceConverter';

  public convertDistance(fromValue: number, fromUnit: string, toUnit: string): number {
    // TODO
    return 0;
  }
}
